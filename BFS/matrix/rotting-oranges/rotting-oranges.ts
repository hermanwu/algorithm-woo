function orangesRotting(grid: number[][]): number {
    // Because multiple start point can compete -> so using bfs only
    
    // A copy of grid to avoid mutating original set.
    const gridCopy = number;
    const result = -1;
    const currentMax = 0;

