export class FeatureDesign {
  getSteps() {
    return [""];
  }

  getMethodology(): { keyPoint: string; example: string }[] {
    return [
      {
        keyPoint: "communcation",
        example: "",
      },
      {
        keyPoint: "universally standard",
        example: "loading, empty, loaded state from different component",
      },
      {
        keyPoint: "break a complex feature into smaller better features",
        example: "text field",
      },
    ];
  }
}
