import { TreeNode } from "./tree-node.class";

export function diameterOfBinaryTree(root: TreeNode | null): number {
  if (root === null) {
    return 0;
  }
  let result = 0;

  return result;
}

export function helper(node: TreeNode) {
  if (node === null) {
  }
}
