export class Retrospective {
  implementationProcess =
    "miss small features, features from Ryan (cloud init calculation and max IOPS calculate)";
  documentationRequirement =
    "annotation is inconsistent, not all changes are from sticksheet";
  communication = "weekly demo is really helpful.";
  improvements = "break down into smaller sets no more than 20 per collections";
  complains;
  failureVsWins;
}
