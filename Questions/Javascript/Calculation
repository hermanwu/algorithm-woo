What is the result of y?

    var x = 3;
    var y = 0;

    function testY() {
        y++;
        return true;
    }

    if (x>=3 && testY()) { y++; }
    if (x>=3 || testY()) { y++; }



What does getRandom() do?

    function getRandom() {
        var tdy = new Date();
        var bigN=tdy.getSeconds()*tdy.getTime();
        bigN *= Math.sqrt(tdy.getMinutes()); console.log(bigN);
        var randN = (bigN % 4) + 1;
        return Math.floor(randN);
    }
